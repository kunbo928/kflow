---
"k-flow": major
---

Rename the temporary recovery contract from `.kflow/work/` and `kflow work` to `.kflow/cursors/` and `kflow cursor`. Existing `.kflow/work/` directories are reported as legacy and are not rewritten automatically.
